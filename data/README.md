# Data

Stub data files for `ironforgedcore.storage.data`.

In consumer repos (`ironforgedbot`, `ironforgedapi`), this directory is
overridden by the `data` submodule, which contains the full game data
JSON files (skill XP tables, boss KCs, clue scroll tiers, raid KCs).
